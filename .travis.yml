language: python

addons:
  apt:
    packages:
      - ldap-utils
      - slapd

matrix:
  include:
    - python: 3.5
      env: TOXENV=py35
    - python: 3.5
      env: TOXENV=lint
    - python: 3.6
      env: TOXENV=format-check
    - python: 3.5
      env: TOXENV=docs

install:
  - pip install tox

script:
  - tox
